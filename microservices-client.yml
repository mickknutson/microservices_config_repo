# Default Profile YAML Property for:
# microservices-client

name: Mick Knutson (v.3.2)
message: Welcome to default YAML properties on HOTFIX
config:
  client:
    message: Some Default YAML Message on HOTFIX
    credentials:
      provider: LDAP
      username: admin7
      password: changeme
      clients: 2
